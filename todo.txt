####
github markup

LUFA :  test existance in makefile 


Docmentation: 
        code
        pinout for different controllers

TP:
        initialization not really thought out
        refactor tp specifics out from PS/2 code
        document registers
        configuration editable at runtime + eeprom store

beautify: 
    for i in *.[ch]; do astyle --style=linux --convert-tabs --lineend=linux --indent=spaces=4  -o -O -K $i ; done


Refactor:
        english / german host layout
        
Layout:
        BU-TECK better?
        enable QWERTY for adopters



debug mode to configure features while running (PinkyDrop, ThumpShift)

startup even if TP is disabled

strg stuck
revert multimodekey

make matrix a matrix
copy paste on mouse middle btn on Win possible?

### DONE ####
move mod4 to shifted mod3 Not very efficient, double usage of modifiers with standard keys is better
F1-12

### Ideas ####

- programming macros on edges?

- prntscreen...

- debug output of layout
- openmoko touchpad?

- multiple layouts -> fixed based layer toggle


### ModKey toggle: Ctrl-Tab needs separate Tab
Ctrl-Alt-Fx
Ctrl-Alt-Del
ctrl-alt       vmware
(ctrl)shift + direction
ctrl-mouseclick

!
win  alt  shft ctrl  | -  mod3 mod4  altgr
     esc  spac tab        back enter del
